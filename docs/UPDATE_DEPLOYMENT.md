# 项目更新部署指南

本指南详细说明如何将最新版本的项目更新到已部署的服务器上。

---

## 📋 更新前准备

### 需要的信息
- ✅ 服务器 IP 地址
- ✅ SSH 访问权限（root 或具有 sudo 权限的账号）
- ✅ 项目路径：`/root/vegetable-basket`
- ✅ 部署方式：Docker Compose
- ✅ 代码管理：Git

### 更新前检查清单
- [ ] 确认服务器可以正常访问
- [ ] 确认 Git 远程仓库已配置
- [ ] 确认本地代码已提交并推送到远程仓库
- [ ] 确认有足够的时间进行更新（建议在业务低峰期）

---

## 第一步：连接到服务器

### Windows 用户（使用 PuTTY）
1. 打开 **PuTTY**
2. 输入服务器 IP 地址
3. 端口：22
4. 点击 "Open" 连接
5. 输入用户名（root）和密码

### Mac/Linux 用户
```bash
ssh root@服务器IP地址
```

---

## 第二步：进入项目目录并检查当前状态

```bash
# 进入项目目录
cd /root/vegetable-basket

# 查看当前服务状态
docker compose ps

# 查看当前 Git 状态
git status

# 查看当前分支和远程仓库
git remote -v
git branch
```

**预期结果**：
- 应该看到三个容器在运行：`cailanzi-mysql`、`cailanzi-backend`、`cailanzi-frontend`
- Git 应该显示当前分支和远程仓库地址

---

## 第三步：备份当前数据（可选但推荐）

虽然您说无需保留现有数据，但为了安全起见，建议先备份：

```bash
# 备份数据库（如果需要）
docker exec cailanzi-mysql mysqldump -uroot -p111111 cailanzi > backup_$(date +%Y%m%d_%H%M%S).sql

# 查看备份文件
ls -lh backup_*.sql
```

**注意**：如果确定不需要备份，可以跳过此步骤。

---

## 第四步：停止当前服务

```bash
# 停止所有服务（数据不会丢失，因为使用了数据卷）
docker compose down

# 验证服务已停止
docker compose ps
```

**预期结果**：应该显示 "No containers" 或空列表。

---

## 第五步：更新代码

### 方法一：使用 Git Pull（推荐）

```bash
# 确保在项目根目录
cd /root/vegetable-basket

# 查看当前分支
git branch

# 拉取最新代码
git pull origin main

# 如果当前不在 main 分支，切换到 main 分支
# git checkout main
# git pull origin main
```

**如果 Git Pull 失败**，可能需要先暂存本地修改：
```bash
# 查看是否有未提交的修改
git status

# 如果有修改，可以选择：
# 方案1：暂存修改（推荐）
git stash
git pull origin main
git stash pop

# 方案2：放弃本地修改（谨慎使用）
git reset --hard
git pull origin main
```

### 方法二：重新克隆（如果 Git 有问题）

```bash
# 备份当前目录（如果需要）
cd /root
mv vegetable-basket vegetable-basket-backup-$(date +%Y%m%d_%H%M%S)

# 重新克隆
git clone https://github.com/Kalstess/vegetable-basket.git

# 进入新目录
cd vegetable-basket
```

---

## 第六步：检查更新内容

```bash
# 查看最新的提交记录
git log --oneline -5

# 检查是否有新的依赖需要安装
# 查看 package.json 是否有变化（前端）
git diff HEAD~1 frontend/package.json

# 查看是否有新的 Docker 配置变化
git diff HEAD~1 docker-compose.yml
```

---

## 第七步：更新前端依赖（如果有变化）

如果 `frontend/package.json` 有变化，需要重新安装依赖：

```bash
# 进入前端目录
cd /root/vegetable-basket/frontend

# 安装新依赖（如果需要）
npm install

# 返回项目根目录
cd ..
```

**注意**：由于使用 Docker 构建，这一步通常不需要，Docker 会自动处理。

---

## 第八步：重新构建并启动服务

### 标准配置（4GB 内存）

```bash
# 确保在项目根目录
cd /root/vegetable-basket

# 重新构建并启动所有服务
docker compose up -d --build
```

### 低内存配置（2GB 内存）

```bash
# 使用低内存配置文件
docker compose -f docker-compose.low-memory.yml up -d --build
```

**命令说明**：
- `up`：启动服务
- `-d`：后台运行
- `--build`：重新构建镜像（重要！必须使用此参数以应用代码更新）

**构建过程**：
- 这个过程可能需要 **10-20 分钟**，请耐心等待
- 会重新编译后端 Java 代码
- 会重新编译前端 Vue 代码
- 会重新构建 Docker 镜像

---

## 第九步：等待服务启动

```bash
# 等待 30 秒让服务完全启动
sleep 30

# 查看服务状态
docker compose ps

# 应该看到三个服务都是 "Up" 状态
```

**如果使用低内存配置**：
```bash
docker compose -f docker-compose.low-memory.yml ps
```

---

## 第十步：查看启动日志

```bash
# 查看所有服务的日志
docker compose logs

# 查看特定服务的日志
docker compose logs backend
docker compose logs frontend
docker compose logs db

# 实时查看日志（按 Ctrl+C 退出）
docker compose logs -f
```

**检查要点**：
- 后端是否正常启动（没有错误信息）
- 前端是否正常启动
- 数据库是否正常连接
- 是否有端口冲突

---

## 第十一步：验证更新

### 1. 检查服务状态

```bash
# 检查容器状态
docker compose ps

# 检查端口监听
netstat -tlnp | grep 8080  # 后端端口
netstat -tlnp | grep 80    # 前端端口
```

### 2. 在浏览器中访问

1. **访问前端管理界面**：
   - 地址：`http://你的服务器IP地址`
   - 应该能看到登录页面
   - 检查是否有新的功能或界面变化

2. **访问后端 API 文档**：
   - 地址：`http://你的服务器IP地址:8080/api/swagger-ui.html`
   - 确认 API 文档正常显示

3. **测试登录功能**：
   - 使用管理员账号登录
   - 确认可以正常登录

### 3. 检查新功能

根据本次更新的内容，测试以下功能：
- ✅ 登录页标题是否为"菜篮子"
- ✅ 侧边栏标题是否为"菜篮子"
- ✅ 收入分析表的区间过滤功能
- ✅ 月运输量趋势表的说明
- ✅ 车辆管理和运输统计的 Excel 导入导出功能

---

## 第十二步：清理旧镜像（可选）

更新完成后，可以清理旧的 Docker 镜像以释放空间：

```bash
# 查看 Docker 镜像
docker images

# 清理未使用的镜像（谨慎使用，会删除所有未使用的镜像）
docker image prune -a

# 或者只清理未使用的资源
docker system prune
```

**⚠️ 注意**：清理镜像前请确保新版本运行正常。

---

## 🔧 常见问题处理

### 问题1：Git Pull 失败 - 有冲突

**解决方法**：
```bash
# 查看冲突文件
git status

# 方案1：保留远程版本（推荐）
git fetch origin
git reset --hard origin/main

# 方案2：手动解决冲突
# 编辑冲突文件，解决冲突后
git add .
git commit -m "解决冲突"
git pull origin main
```

### 问题2：构建失败 - 内存不足

**解决方法**：
```bash
# 检查内存使用
free -h

# 如果内存不足，使用低内存配置
docker compose -f docker-compose.low-memory.yml up -d --build

# 或者清理 Docker 资源
docker system prune -a
```

### 问题3：服务启动失败

**解决方法**：
```bash
# 查看详细日志
docker compose logs backend | tail -50
docker compose logs frontend | tail -50
docker compose logs db | tail -50

# 检查配置文件
cat docker-compose.yml

# 检查端口占用
netstat -tlnp | grep 80
netstat -tlnp | grep 8080
```

### 问题4：前端无法访问

**检查步骤**：
1. 检查容器是否运行：`docker compose ps`
2. 检查防火墙：`firewall-cmd --list-ports`
3. 检查安全组（云服务器）
4. 查看前端日志：`docker compose logs frontend`

### 问题5：数据库连接失败

**解决方法**：
```bash
# 检查数据库容器
docker compose ps db

# 查看数据库日志
docker compose logs db

# 检查数据库密码配置
grep MYSQL_ROOT_PASSWORD docker-compose.yml
grep SPRING_DATASOURCE_PASSWORD docker-compose.yml
```

---

## 🔄 回滚方案（如果更新失败）

如果更新后出现问题，可以快速回滚到之前的版本：

```bash
# 1. 停止当前服务
docker compose down

# 2. 回退到之前的 Git 提交
cd /root/vegetable-basket
git log --oneline  # 查看提交历史
git checkout <之前的提交ID>

# 3. 重新构建并启动
docker compose up -d --build

# 或者使用备份的目录（如果使用了方法二）
cd /root
rm -rf vegetable-basket
mv vegetable-basket-backup-* vegetable-basket
cd vegetable-basket
docker compose up -d --build
```

---

## 📝 更新检查清单

更新完成后，请确认：

- [ ] 所有服务正常运行（`docker compose ps`）
- [ ] 前端可以正常访问
- [ ] 后端 API 可以正常访问
- [ ] 可以正常登录系统
- [ ] 新功能正常工作
- [ ] 没有错误日志
- [ ] 数据库连接正常

---

## 🚀 快速更新脚本（可选）

您也可以创建一个更新脚本，简化更新流程：

```bash
# 创建更新脚本
cat > /root/vegetable-basket/update.sh << 'EOF'
#!/bin/bash
set -e

echo "=========================================="
echo "  菜篮子系统更新脚本"
echo "=========================================="
echo ""

cd /root/vegetable-basket

echo "1. 停止当前服务..."
docker compose down

echo "2. 更新代码..."
git pull origin main

echo "3. 重新构建并启动服务..."
docker compose up -d --build

echo "4. 等待服务启动..."
sleep 30

echo "5. 检查服务状态..."
docker compose ps

echo ""
echo "=========================================="
echo "  更新完成！"
echo "=========================================="
echo ""
echo "请访问 http://$(hostname -I | awk '{print $1}') 验证更新"
EOF

# 添加执行权限
chmod +x /root/vegetable-basket/update.sh
```

**使用方法**：
```bash
cd /root/vegetable-basket
./update.sh
```

---

## ⚠️ 重要提示

1. **更新时机**：建议在业务低峰期进行更新
2. **备份数据**：虽然您说无需保留数据，但建议至少备份数据库
3. **测试环境**：如果有条件，建议先在测试环境验证更新
4. **监控日志**：更新后持续观察日志，确保没有错误
5. **版本标记**：更新后可以记录更新时间和版本号

---

## 📞 需要帮助？

如果更新过程中遇到问题：
1. 查看日志：`docker compose logs`
2. 检查服务状态：`docker compose ps`
3. 查看本文档的"常见问题处理"部分
4. 记录错误信息，联系技术支持

---

**祝您更新顺利！** 🚀

