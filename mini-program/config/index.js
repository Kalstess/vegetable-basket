// 小程序配置
// 注意：真机调试时，需要将 localhost 改为开发机器的实际 IP 地址
// 例如：baseURL: 'http://192.168.1.100:8080/api'
// 可以在微信开发者工具 -> 设置 -> 项目设置 -> 本地设置 -> 不校验合法域名 中临时关闭域名校验

const config = {
	// 开发环境：使用 localhost（仅模拟器可用）
	// 真机调试：需要改为实际 IP 地址，注意要包含端口号 8080
	// 格式：http://IP地址:8080/api
	baseURL: 'http://192.168.1.108:8080/api',
	
	// 如果需要支持真机调试，可以取消注释下面这行，并修改为你的实际 IP
	// baseURL: 'http://192.168.1.100:8080/api',
};

module.exports = config;


